name: money_mind_mobile
description: "A new Flutter project."

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter

  http: ^1.2.1
  shared_preferences: ^2.2.3
  intl: ^0.19.0
  flutter_secure_storage: ^9.0.0
  path_provider: ^2.1.2 # ← AÑADIDO
  provider: ^6.1.2       # ← AÑADIDO
  cupertino_icons: ^1.0.8
  crypto: ^3.0.3
  fl_chart: ^0.65.0
  dio: ^5.4.0
  web_socket_channel: ^3.0.3 # dependencia para WebSocket 
  signalr_netcore: ^1.4.3 # Agrega esta línea

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
